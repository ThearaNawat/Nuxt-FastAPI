apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config
  namespace: erp

data:
  APP_ENV: "development"
  DATABASE_URL: "postgresql://postgres:123456@localhost:5432/erp_db"
  REDIS_URL=redis://localhost:6379/0
  