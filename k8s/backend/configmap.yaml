apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config
  namespace: erp

data:
  APP_ENV: "development"
  DATABASE_HOST: "postgres-service"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "erp_db"
  